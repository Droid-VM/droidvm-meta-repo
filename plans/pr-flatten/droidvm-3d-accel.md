## droidvm-3d-accel (META, /root/gitrs/DroidVM/DroidVM_3daccel_gfxstream) — base `4b825dc` (empty tree; root commit bf63337) .. `9cc6ef4` (wip/3d-accel)  179 original commits, 108 net files (+42327 lines)

Notes on the range:
- Every commit is by HuJK. Org default branch `origin/wip-3d-accel` = `4b0a5a3` (the 2nd commit) — see "base rebase" below.
- **HEAD moved during this analysis**: `d4a4f3d` "build: mesa-cross moves to its own repo (Droid-VM/mesa-cross), cloned like any component" landed on wip/3d-accel while the report was being written (2026-08-17). It DELETES `mesa-cross/*` (6 files) and `mesa-variants.sh` from this repo (history carried to Droid-VM/mesa-cross by filter-repo), adds `clone_at mesa-cross` to `1_build_crosvm_prepare.sh`/`lib_mesa_build.sh`, moves `mesa_worktree()` into `lib_mesa_build.sh`, points `deploy/guest/provision*.sh` at `mesa-cross/mesa-variants.sh`, adds `/mesa-cross/` to .gitignore and rewrites README's guest-mesa section. The tables below describe the requested range (..9cc6ef4); the effect of d4a4f3d is spelled out in "Open questions" #1 — recommend flattening from d4a4f3d, in which case commit G4 shrinks and the mesa-cross/mesa-variants commits net to zero *here* and belong to the mesa-cross repo's flatten.
- Uncommitted working tree at analysis time (IGNORED, per instructions): `plans/POOL_DESIGN.md` deleted; untracked `CLAUDE.md`, `info.txt`, `plans/ACCEPTANCE_REPORT_R5_20260817.md`. Earlier in the session `lib_mesa_build.sh`, `8_build_guest_mesa_*.sh`, `mesa-cross/Dockerfile.mesa-cross`, `provision*.sh`, `README.md`, `.gitignore`, `1_build_crosvm_prepare.sh` were modified uncommitted — that work became d4a4f3d.

### Proposed flattened commits (in order)

| # | bucket | proposed subject | folds original commits | net files (paths) | notes |
|---|---|---|---|---|---|
| 1 | L | licensing: GPL-3.0-or-later with additional permissions to take the work upstream; attribute to the Droid-VM organization | 37560d3, db999d3 | ADDITIONAL-PERMISSIONS, CONTRIBUTING.md, LICENSE.GPL-3.0, LICENSING.md | No DroidVM-authored .sh/.py/.md in this repo carries an SPDX header although LICENSING.md defines DroidVM material by SPDX tag — decide whether L adds `SPDX-License-Identifier: GPL-3.0-or-later` to the scripts. poolprobe/*.c are `GPL-2.0` (kernel modules); linux/0001 patch and the ICD snapshots are upstream/third-party (see LICENSING §"third-party"). |
| 2 | M | pipeline: meta repo skeleton — numbered build scripts (crosvm soong tree, EDK2, APK), branch-chain resolution, README, .gitignore | bf63337 (build_crosvm/collect/crosvm_build_prepare/out_to_adb/run_ubuntu + .gitignore), 1014738, 399146d, ea1c4f4, 8103642 (renumber; 3_/6_/7_), 7389713, e23936e, 9817d24 (1_ part), 997a494, cfc3504 (README), bc1f0c2 (README), a70011e (README), 60c83f5 (README), a2c6e12 (lib_branch LEGACY comment / .gitignore) | .gitignore (SPLIT), 1_build_crosvm_prepare.sh, 2_build_crosvm.sh, 2-1_collect_crosvm.sh, 2-2_crosvm_out_to_adb.sh, 2-3_crosvm_run_ubuntu.sh, 3_build_edk2.sh, 6_build_apk_prepare.sh (SPLIT), 7_build_apk.sh, lib_branch.sh, README.md (SPLIT) | 7389713→e23936e→997a494 are three successive rewrites of branch selection; only lib_branch.sh's chain survives. 2-2/2-3 hard-code an adb serial and `/data/local/tmp/start.sh` (dev helpers). lib_branch.sh: `TRUNK=wip/3d-accel`, `LEGACY=wip/3d-accel-gfxstream` — see Open questions #3. |
| 3 | G | pipeline: build the gunyah host modules (host-share / udmabuf / gh_unmovable) and stage them, their descr pages and match.json into the APK prebuilts | 8103642 (4_ + 6_ module hunk), 08d0d47 | 4_build_gunyah_host.sh; 6_build_apk_prepare.sh hunks (SPLIT from #2) | Host side of gunyah blob share/accept + udmabuf (shared by all routes) and the app's module-manager metadata. Also touched by 7389713/e23936e/997a494 (branch plumbing, folded in #2). |
| 4 | G | turnip: build the host Vulkan driver from the Droid-VM/turnip fork (gen8, KGSL) and ship it in the APK | 8103642 (5_ part), 7bc344c, eff42e6, 9f0de1c, 2865f35 | 5_prepare_turnip.sh; 6_build_apk_prepare.sh turnip hunk (SPLIT from #2); .gitignore `/turnip/*` lines (SPLIT from #2) | Host turnip is the GPU driver under gfxstream AND venus host (VENUS_BRINGUP: "host Vulkan 一律 turnip"); drm2kgsl does not need it. turnip/patches/000{1,2,3} (7bc344c, 9f0de1c) were removed again in 2865f35 (moved into the Droid-VM/turnip fork) → net zero. |
| 5 | G | guest-patches: guest kernel virtio-gpu/gunyah patch series as in-tree reference, plus the pre-DKMS module-load unit | 57e76c5, 9817d24 (0004/0005 + README), cfc3c59 (service) | guest-patches/linux/0001-virtio-gpu-Add-Gunyah-Resource-Manager-support-for-m.patch, guest-patches/linux/0002-virtio-gpu-gunyah-gate-memparcel-accept-on-gunyah_gu.patch, guest-patches/linux/0003-virtio-gpu-gunyah-demote-per-blob-create-entry-trace.patch, guest-patches/linux/0004-virtio-gpu-take-the-blob-dma-buf-import-path-even-wi.patch, guest-patches/linux/0005-virtio-gpu-don-t-detach-resources-from-a-context-the.patch, guest-patches/linux/README.md, guest-patches/droidvm-gpu-modules.service | README itself says the shipped series is `droidvm-guest-additions/patches/<series>/` and this is a mirror. 0001 is authored by **lateautumn233** inside the patch file. Candidate for dropping (see below). |
| 6 | G | build: cross-build the guest mesa variants in a container and package them (+ guest additions) as .deb into dist-guest/ | b61b21b, ce0c3ee (infra), bc1f0c2, 20c0c77 (--network=host), 58b7553 (readelf check), 8068223, 82a3c95 (siblings Conflicts/Replaces), acbb43c, a70011e, 64d57d1, 6b425ef, cb30848, d2cb940, 1abda84, 8103642 (8_ rename) | mesa-cross/Dockerfile.mesa-cross, mesa-cross/aarch64-linux-gnu.meson, mesa-cross/ubuntu.sources, mesa-cross/build.sh, mesa-cross/build-in-container.sh, mesa-cross/package.sh, mesa-variants.sh (SPLIT), lib_mesa_build.sh (SPLIT), lib_dist.sh (SPLIT), 9_build_guest_addition.sh, .gitignore lines `mesa-guest-*.tar.gz`, `mesa-guest-*.deb`, `/mesa-gfxstream/`, `/mesa-drm2kgsl/`, `dist-guest/` (SPLIT from #2), README "Guest mesa" section (SPLIT) | Includes the environment-delivery design (environment.d / profile.d / /etc/environment / system.conf.d) in package.sh. Net-zero inside: 8_build_guest_mesa.sh (native), 8_build_guest_mesa_cross.sh, 8_build_guest_mesa_fedora.sh + Dockerfile.mesa-fedora + rpm branch (acbb43c→a70011e), mesa-cross/build-host.sh (a70011e→64d57d1), MESA_NATIVE path, KWIN_FORCE_SW_CURSOR (6b425ef→cb30848). **After d4a4f3d all mesa-cross/* and mesa-variants.sh leave this repo** → this commit becomes lib_mesa_build.sh + lib_dist.sh + 9_ + .gitignore/README bits only. |
| 7 | G | deploy: provision a guest for one route over ssh, re-establish the phone after a reboot, and SETUP.md | b34f5d6 (SETUP), 7475e70 (SETUP), 6242ac1 (bringup.sh), d31d315 (bringup verify libvirglrenderer), cfc3504 (SETUP), a322425, 20c0c77 (provision.sh), bc1f0c2 (SETUP), 60c83f5 (SETUP/bringup/provision), 9477084, 82a3c95 (provision), a70011e (SETUP/provision), a2c6e12 (bringup/provision) | deploy/SETUP.md (SPLIT), deploy/guest/provision.sh, deploy/gfxstream/bringup.sh (SPLIT) | bringup.sh lives under deploy/gfxstream/ but pushes both routes' binaries and loads the host modules → common. Stale: bringup.sh still pushes `run_ubuntu_gfx_prealloc.sh` (renamed to `_fusion` in fbde26d) and lists only three gfx launchers; SETUP.md still documents `run_ubuntu_gfx_prealloc.sh`, `gfx-guest-mb`, "手機不可重開機", §4 says drm2kgsl has no guest-alloc path (it has since 2bfa1a9). |
| 8 | G | bench: vkmark + Minecraft benchmark harness for every route — fixed scene over VNC, clock pinning, fps template reader, host preflight, reboot watchdog, evidence capture | d1a195d, ae6f360, 9a33620, 925a238, e4e7ca9, edd7c95, 6b1cff4, 7ec8cd2, 104e6bd (bench_all/bench_one/glyphs), bc4a5a4, b5bc340, ceec69f, 7558dea (bench_all/sweep), 8b9a5a9, 0070694, 2c03cf0, e0a0421, 70c2786 (bench_all/preflight), 61873d0 (bench_all), 0d17956 (bench_all poll), 83941b3 (bench_all/preflight), 20032c4 (bench_all/bench_one/preflight/mc_bench/glyphs), 82a3c95 (bench_all mesa_ensure, poolinfo.c), bcba917 (bench_all/preflight wording), 60c83f5 (mc_bench) | deploy/gfxstream/bench_all.sh (SPLIT), bench_one.sh, mc_bench.sh (SPLIT), mc_read_fps.py, mc_fps_glyphs.json, sweep_all_routes.sh (SPLIT), deploy/gfxstream/harness/README.md, harness/mc_lib.sh, harness/vm_restart.sh, harness/preflight.sh, harness/threadcpu.sh, harness/kgsl_trace.sh, harness/poolinfo.c | Route-agnostic despite the `deploy/gfxstream/` path (bench_all CONFIGS covers the 4 gfxstream configs + drm2kgsl; mc_bench picks the MC launcher by installed variant). Consider renaming to `deploy/bench/` in the flatten. Depends on guest-side scripts not in this repo (`/home/droidvm/launch_mc_vk.sh`, `launch_mc_kgsl_nctx.sh`, `java_wrap.sh`). |
| 9 | G | deploy: reserve-pool and VMM-death diagnostics (pool leak rate, poolprobe kernel modules, SIGKILL blast radius, FD_SETSIZE repro) | 20032c4 (pool_leak_rate.sh), 7f62051 (poolprobe/*), 70c2786 (fdsetsize_repro.sh), 61873d0 (fdsetsize), 0d17956 (sigkill_blast_radius.sh), 83941b3 (sigkill), bc9d54e (sigkill), bcba917 (fdsetsize wording) | deploy/gfxstream/pool_leak_rate.sh, deploy/gfxstream/poolprobe/README.md, poolprobe/build.sh, poolprobe/ghhr_probe.c, poolprobe/ghhr_sites.c, deploy/gfxstream/sigkill_blast_radius.sh, deploy/gfxstream/fdsetsize_repro.sh | Kept separate so it can be dropped wholesale — all four are one-investigation tools (see "consider dropping"). poolprobe reads gh_hugepage_reserve private state via kallsyms; arguably belongs in the gh-hugepage-reserve repo. |
| 10 | G | plans: memory-model and reserve-pool design docs (CMDLINE_V2, SYNC_AND_POOL_V2, GUEST_ALLOC_PERF, DYNAMIC_POOL_PLAN, POOL_RECLAIM_PLAN, POOL_DESIGN) | b34f5d6 (CMDLINE_V2/GUEST_ALLOC_PERF/SYNC_AND_POOL_V2), 82a3c95 (DYNAMIC_POOL_PLAN + tweaks), a70011e (CMDLINE tweak), bc9d54e, 1a34c93, 08e058d, 6d71c4f (RECLAIM), 5ad2629, 1d12d24, 12f23c6, 69d0466, 1bf52a7, 38253e0 (POOL_DESIGN), cb5e5a7, 69b226d, 6b20c09, 6b2d751, 9be6785, 587d306, 690add4, 0c2380d, 903c6f9, cc504bf, 0f68d46, d22e08c, 92b2074, aa1415e, 198c041, 0e57913, 6006aeb, 1020179, a9c502b, ba7b443, 2ae0337, 4e3f700, 2cb6f4b, 80663b1 | plans/CMDLINE_V2.md, plans/SYNC_AND_POOL_V2.md, plans/GUEST_ALLOC_PERF.md, plans/DYNAMIC_POOL_PLAN.md, plans/POOL_RECLAIM_PLAN.md, plans/POOL_DESIGN.md | These are the "durable" design docs of the common infra (crosvm CLI 3-axis model, VmAccept::Sync + pool formalization, guest-alloc pool, growable pool, pool reclaim, gh-hugepage-reserve v2 design). GUEST_ALLOC_PERF is titled "官方 gfxstream 路線" but describes the guest-alloc pool that drm2kgsl/venus later reuse → G. **POOL_DESIGN.md is deleted in the working tree** (uncommitted) — is it moving to gh-hugepage-reserve? |
| 11 | G | plans: pool-leak investigation survey, design derivation notes and dated evidence packs | bf63337 (PAGE_DEATH_EVIDENCE), 9f39841, aec5575, d8fe2d3, 425502c, e784098, eb8ac29, 55e29b6, 8a54e3d, 347428f, bd43c00, 99e82f8, 8d4bdad, a32588c, 6e4129b, 69307cf, 1442812, 7f62051 (survey), 3ceab56, 045a523, 0bc59f2, 1c4d5b3, 2d7f131, 4ae9f9f, 0d03fdc, a5ccfab, 83c799a, 6e6c470, 7e77376, dbc21de, 3e8e5d9, 94d41b5, a834c0e, 7f25864, 4acc0f3, 5d1e61c, f506650, 7a6498f, bfbb18b, f3fd557, 21d020d, 5a510d2, 38253e0 (headers), 9cc6ef4 (ACCEPTANCE_REPORT) | plans/POOL_HANDOFF_SURVEY.md, plans/POOL_STATE_MODEL.md, plans/POOL_ARCH_V2.md, plans/POOL_OBJECT_API.md, plans/POOL_PROPOSAL_V3.md, plans/POOL_GREENFIELD.md, plans/PAGE_DEATH_EVIDENCE.md, plans/ACCEPTANCE_REPORT_20260816.md | 44 of the 179 commits are this one investigation (08-06..08-09). Five of these docs open with "現行設計見 POOL_DESIGN.md(單一來源)... 這份是推導過程"; POOL_HANDOFF_SURVEY says "§4.9 之前的結論都不要照著動手". Separate commit so it can be dropped as a unit. |
| 12 | D | drm2kgsl: guest mesa variant (turnip over vdrm) and the native-context launcher | ce0c3ee (kgsl variant), d31d315 (deploy/kgsl/run_kgsl_nctx.sh), 82c1d50, 58b7553 (EDK2 boot), 509c243, 2bfa1a9, 0a19d89, cce7feb, a2c6e12 (kgsl→drm2kgsl rename), 60c83f5, 7bd867f, 7558dea (CROSVM_LOG), 82a3c95 (DT node rename), bcba917 (wording), a70011e (8_build_guest_mesa_drm2kgsl.sh), 64d57d1, ceec69f (mc_bench launcher pick) | deploy/drm2kgsl/run_drm2kgsl_nctx.sh, 8_build_guest_mesa_drm2kgsl.sh; SPLIT hunks: mesa-variants.sh drm2kgsl/kgsl lines, lib_mesa_build.sh case, lib_dist.sh line, README/SETUP §2 rows, bench_all.sh CONFIGS row + purge list, mc_bench.sh launcher selection, sweep_all_routes.sh phase 2, bringup.sh crosvm_drm2kgsl push, provision.sh driverName text | 2bfa1a9→0a19d89 (drop drm-host-mb, then keep it at 8 MiB) partly cancel; final launcher has `--pre-alloc drm-host-mb=8,gpu-guest-mb=1024`, `udmabuf=true`, EDK2 boot. |
| 13 | D | drm2kgsl: early native-context plan, paged-arena design, legacy start_nctx.sh and turnip-virtio tree snapshot | bf63337 (ARENA_V2_PLAN, NATIVE_CONTEXT_PLAN, start_nctx.sh, guest-patches/mesa-26.0.3 freedreno files), 399146d (move into plans/) | plans/NATIVE_CONTEXT_PLAN.md, plans/ARENA_V2_PLAN.md, start_nctx.sh, guest-patches/mesa-26.0.3/README.md, guest-patches/mesa-26.0.3/src/freedreno/common/msm_proto.h, …/src/freedreno/drm/virtio/virtio_bo.c, …/virtio_device.c, …/virtio_priv.h, …/src/freedreno/vulkan/tu_knl_drm_virtio.cc | All four items are pre-v3 (2026-07-11): start_nctx.sh passes `context-types=drm` only, builds its own tap/NAT/dnsmasq, `LD_PRELOAD=libsimpledump.so` — superseded by #12's launcher (d31d315 says so). Snapshot is superseded by mesa `wip/3d-accel-drm2kgsl`. Consider dropping the whole commit. |
| 14 | X | gfxstream: guest mesa variant, one launcher per memory configuration (purepool / fusion / runtime-share / guest-alloc), streaming-trace launcher and VK_KHR_display test | b34f5d6 (run_ubuntu_gfx*.sh), 7475e70, 6242ac1 (run_stream_trace.sh, vkdisplay_test.c), ae3d9b5, d31d315 (ulimit -l), fbde26d, 233e634, 4f94316, 104e6bd (host-share insmod), 7558dea (CROSVM_LOG), ce0c3ee (gfxstream variant), a70011e (8_build_guest_mesa_gfx.sh), 64d57d1 | deploy/gfxstream/run_ubuntu_gfx.sh, run_ubuntu_gfx_fusion.sh, run_ubuntu_gfx_guestalloc.sh, run_ubuntu_gfx_purepool.sh, run_stream_trace.sh, vkdisplay_test.c, 8_build_guest_mesa_gfx.sh; SPLIT hunks: mesa-variants.sh gfxstream lines, lib_mesa_build.sh case, README/SETUP §1 rows, bringup.sh gfx launcher push list, sweep_all_routes.sh phase 1 | run_ubuntu_gfx_prealloc.sh→_fusion.sh and _sync.sh→_guestalloc.sh are renames (fbde26d). run_stream_trace.sh still exports `NCTX_GFX_POOL_MB`/`NCTX_GFX_GUEST_POOL_MB` and hard-codes debug log level (stale env names?). vkdisplay_test.c was written for one crash hunt (candidate for dropping). |
| 15 | X | gfxstream: guest ICD / zink source snapshots (l233-mesa canonical tree, on-guest mesa-26.0.3) | cfc3c59 (ResourceTracker), f3a2990, 3410861, fded20b, 4a18838, 8eacc22, 98fc47b, 19b6675, 2145e17 | guest-patches/l233-mesa/README.md, guest-patches/l233-mesa/src/gallium/drivers/zink/zink_screen.c, …/src/gfxstream/codegen/scripts/cereal/functable.py, …/src/gfxstream/guest/platform/drm/DrmVirtGpuBlob.cpp, …/DrmVirtGpuBlobMapping.cpp, …/src/gfxstream/guest/vulkan/gfxstream_vk_cmd.cpp, …/src/gfxstream/guest/vulkan_enc/ResourceTracker.cpp, …/ResourceTracker.h, guest-patches/mesa-26.0.3/src/gallium/drivers/zink/zink_screen.c, guest-patches/mesa-26.0.3/src/gfxstream/guest/vulkan_enc/ResourceTracker.cpp | ~26k lines of whole-file copies of upstream (Apache-2.0 gfxstream / MIT mesa) files with local edits, from a tree "not pushable by this org". Superseded by Droid-VM/mesa `wip/3d-accel-gfxstream` (which 8_build_guest_mesa_gfx.sh builds). Strong candidate for dropping. |
| 16 | X | gfxstream: arena/folio-backing plan, ship plan, seqno dead ends, crosvm launch reference and the debug-loop notes | 4b0a5a3, ce734aa, 399146d (CROSVM_3D_LAUNCH_REFERENCE), 551915e, eb8b452, d52e926, 6f862e8, de458eb, 2fd2369, 4c6d0be, c50d255, ecf79d1, 6d71c4f (GFX_SHIP), 7596925 | plans/GFXSTREAM_ARENA_PLAN.md, plans/GFX_SHIP_PLAN.md, plans/SEQNO_DEAD_ENDS.md, plans/CROSVM_3D_LAUNCH_REFERENCE.md, debugloop.md | GFXSTREAM_ARENA_PLAN + SEQNO_DEAD_ENDS are durable "why". GFX_SHIP_PLAN is a status doc (mostly durable "why not"). CROSVM_3D_LAUNCH_REFERENCE documents the `blob_mode=guest-accept` / `GFXSTREAM_ARENA_MB` era and says "不是 source of truth" — stale. debugloop.md refers to scratchpad tools not in the repo. |
| 17 | V | venus: third guest mesa variant, provisioning, route survey and bring-up worklog | efc1fe3, a24c655, 9d7ba39, f290df3, 2867932, dbef5b2, 1c47873, fcc31be, 7b1293e, 9cc6ef4 (provision-any.sh) | 8_build_guest_mesa_venus.sh, deploy/guest/provision-any.sh, plans/VENUS_SURVEY.md, plans/VENUS_BRINGUP.md; SPLIT hunks: mesa-variants.sh venus lines (branch/meson/icd/siblings + `-venus` strip), lib_dist.sh venus check (l.53), lib_mesa_build.sh case, .gitignore `/mesa-venus/` | provision-any.sh is provision.sh with `venus` added to one `case` (its usage text still says gfxstream\|drm2kgsl) — fold into provision.sh instead of a second file. No `deploy/venus/` launcher exists (venus is launched via the app/daemon per VENUS_BRINGUP). README at 9cc6ef4 has no venus row (d4a4f3d adds one). |

### Other-author commits (keep authorship)
None as git commits — all 179 are HuJK. Embedded third-party content to be aware of:
- `guest-patches/linux/0001-virtio-gpu-Add-Gunyah-Resource-Manager-support-for-m.patch` — `From: lateautumn233 <lateautumn233@foxmail.com>` inside the patch file. If #5 is kept, keep that patch's From: line intact (or apply it as its own commit with lateautumn233 authorship in the guest-additions repo, where it actually ships).
- `guest-patches/l233-mesa/` — snapshot of lateautumn233's Crosvm-Android/mesa `3d-accel` tree files (name "l233"); the files themselves are Google gfxstream (Apache-2.0) / Mesa (MIT) with local edits, no per-hunk attribution.
- `.gitignore` names scratch dirs `lateautumn233_attemp/`, `hujk_attemp/`, `kgsl-ref/`, `virglrenderer-upstream/` (untracked scratch, nothing to preserve).

### Nets to zero / disappears in flatten
- `turnip/patches/0001-kgsl-track-sparse-vma…`, `0002-kgsl-tolerate-KHR_display…`, `0003-kgsl-poll-timestamps…` — added 7bc344c, annotated 9f0de1c, deleted 2865f35 (moved into Droid-VM/turnip `gen8`). 9f0de1c's measurement text (+34% frames) survives only in the commit message.
- `crosvm_build_prepare.sh` (bf63337, 1014738) → replaced by `1_build_crosvm_prepare.sh` (399146d).
- Renames only: `build_crosvm.sh`→`2_build_crosvm.sh`, `collect_crosvm.sh`→`3_collect_crosvm.sh`→`2-1_…`, `crosvm_out_to_adb.sh`→`3-1_`→`2-2_`, `crosvm_run_ubuntu.sh`→`3-2_`→`2-3_`, `4_build_apk_prepare.sh`→`6_…`, `5_build_apk.sh`→`7_…`, `6_build_guest_mesa.sh`→`8_build_guest_mesa.sh` (then deleted), root `*.md` → `plans/*.md` (399146d).
- `8_build_guest_mesa.sh` (native aarch64 build; 399146d..bc1f0c2) and `8_build_guest_mesa_cross.sh` (b61b21b..20c0c77) → deleted a70011e, replaced by `8_build_guest_mesa_{gfx,drm2kgsl}.sh` + `lib_mesa_build.sh`.
- `8_build_guest_mesa_fedora.sh`, `mesa-cross/Dockerfile.mesa-fedora`, package.sh's rpm branch (acbb43c) → deleted a70011e. Only package.sh's deb half survives.
- `mesa-cross/build-host.sh` (a70011e) → deleted 64d57d1, replaced by `mesa-cross/build.sh`.
- `MESA_NATIVE=1` path in build-in-container.sh (bc1f0c2) → removed a70011e.
- `KWIN_FORCE_SW_CURSOR=1` in package.sh (6b425ef) → removed cb30848 (package.sh now documents *why not*).
- `deploy/gfxstream/run_ubuntu_gfx_prealloc.sh`→`run_ubuntu_gfx_fusion.sh`, `run_ubuntu_gfx_sync.sh`→`run_ubuntu_gfx_guestalloc.sh` (fbde26d renames); `vm-accept=sync` flag removed 7475e70 before that.
- `deploy/kgsl/run_kgsl_nctx.sh` → `deploy/drm2kgsl/run_drm2kgsl_nctx.sh` (a2c6e12); `--pre-alloc kgsl-mb`/`drm-guest-mb`/`gfx-guest-mb` flag names churn (2bfa1a9, 0a19d89, cce7feb, 4f94316) → final `drm-host-mb`/`gpu-guest-mb`; `CROSVM_KGSL_DIAG`→`CROSVM_DRM2KGSL_DIAG` (7bd867f); "hijack module" wording (bcba917).
- Branch pinning: hardcoded `3d-accel-gfxstream` (399146d) → `wip/3d-accel-gfxstream` (7389713) → `$(git rev-parse --abbrev-ref HEAD)` (e23936e) → chain in lib_branch.sh (997a494).
- Intermediate doc states: GFXSTREAM_ARENA_PLAN v1 (4b0a5a3) rewritten by ce734aa; POOL_HANDOFF_SURVEY's isolate recommendation made/withdrawn/reinstated/withdrawn (aec5575, 8d4bdad, a32588c, 69307cf) — the file keeps them all "in order with what refuted each" (that is the doc's stated style, so not net-zero on disk).
- If flattening from d4a4f3d (see Open questions #1): all `mesa-cross/*` and `mesa-variants.sh` content nets to zero in this repo.

### Consider dropping from the PR
(owner decides; grouped by proposed commit)
- **#15 whole** — `guest-patches/l233-mesa/*` and the two gfxstream/zink files under `guest-patches/mesa-26.0.3/`: 26k-line whole-file snapshots of upstream code, superseded by the mesa `wip/3d-accel-gfxstream` branch that 8_build_guest_mesa_gfx.sh actually builds; l233-mesa README describes a docker tree/workflow that no longer applies.
- **#13 whole** — `start_nctx.sh` (pre-v3 launcher, superseded by deploy/drm2kgsl/), `guest-patches/mesa-26.0.3/src/freedreno/*` + README (snapshot of a no-git tree, superseded by mesa `wip/3d-accel-drm2kgsl`), `plans/NATIVE_CONTEXT_PLAN.md` (2026-07-11 status: references start_nctx.sh, virglrenderer commit 147dea5, "待人工執行"), `plans/ARENA_V2_PLAN.md` (paged-arena run-list protocol; the shipped drm2kgsl guest-alloc went through the gpu-guest-mb drm_buddy pool instead — check whether the msm_proto extension is still live before keeping).
- **#5** — `guest-patches/linux/*` (mirror of the DKMS series in droidvm-guest-additions, which the README says is what ships) and `droidvm-gpu-modules.service` (hand insmod unit from before the DKMS package). Keep only if the "in-tree reference" is wanted; if kept, note lateautumn233's authorship of 0001.
- **#11 whole** — POOL_HANDOFF_SURVEY.md (1046 lines, five refuted mechanisms; the surviving answer, the `__GFP_MOVABLE` allocation filter, is §4.9–4.10 and is also recorded in POOL_RECLAIM_PLAN/GFX_SHIP_PLAN), POOL_STATE_MODEL / POOL_ARCH_V2 / POOL_OBJECT_API / POOL_PROPOSAL_V3 / POOL_GREENFIELD (self-described derivations of POOL_DESIGN.md), PAGE_DEATH_EVIDENCE.md (2026-07-10 evidence pack), ACCEPTANCE_REPORT_20260816.md (dated 3-device acceptance table; an R5 report already exists untracked).
- **#9 whole** — pool_leak_rate.sh (measurement behind POOL_RECLAIM_PLAN §0), poolprobe/* (two one-shot kernel modules for a closed investigation; belong with gh-hugepage-reserve if anywhere), sigkill_blast_radius.sh (deliberately SIGKILLs crosvm; one experiment), fdsetsize_repro.sh (repro for an already-fixed crosvm VNC FD_SETSIZE abort — could be kept as a regression test).
- **#16 partial** — plans/CROSVM_3D_LAUNCH_REFERENCE.md (stale dev launch reference: `--hypervisor gunyah[blob_mode=guest-accept]`, `GFXSTREAM_ARENA_MB`, `/data/local/tmp/start_ga_folio.sh`), debugloop.md (points at scratchpad/devvm.sh, gfxcheck.sh, gfxrun.sh, rfb_grab.py, grab2.py which are not in the repo; the trap list is useful but is session notes), GFX_SHIP_PLAN.md (status doc; the durable parts are already in SEQNO_DEAD_ENDS/POOL_RECLAIM_PLAN).
- **#14 partial** — deploy/gfxstream/vkdisplay_test.c (VK_KHR_display ladder for one phone-reset hunt), run_stream_trace.sh (debug launcher; stale `NCTX_GFX_*` env names, hard-coded debug log level, `--pre-alloc gfx-host-mb=256,gpu-guest-mb=1024`).
- **#17 partial** — plans/VENUS_BRINGUP.md is a session worklog ("session 換手先讀這裡"); VENUS_SURVEY.md is the durable half. deploy/guest/provision-any.sh should be folded into provision.sh (venus in the case) rather than shipped as a copy.
- **#7 partial** — bringup.sh has Chinese status strings ("adb 沒回來", "環境就緒") and hard-coded serial; keep but tidy.
- Not dropping but noteworthy: every deploy/ script hard-codes `172.22.74.2:5568`, `root@172.22.68.12`, the qcow2 filename, boot-cache UUID `526795fd-…`, tap `vm526795fd-0`, MAC, `/home/droidvm`; bench scripts take PHONE/GUEST env, launchers/bringup do not.

### SPLIT NEEDED files
| file | primary | hunks elsewhere |
|---|---|---|
| `.gitignore` | #2 M | `/turnip/*` (4 lines) → #4; `mesa-guest-*.tar.gz`, `mesa-guest-*.deb`, `/mesa-gfxstream/`, `/mesa-drm2kgsl/`, `dist-guest/` → #6; `/mesa-venus/` → #17. (d4a4f3d adds `/mesa-cross/` → #2/#6.) |
| `6_build_apk_prepare.sh` | #2 M | gunyah_host_mod .ko / descr pages / match.json / style.css blocks (l.34–70) → #3; turnip block (l.72–77) → #4 |
| `1_build_crosvm_prepare.sh` | #2 M | `clone_at droidvm-guest-additions` → #6 (9_ needs it); `clone_at mesa` → #6 (mesa worktrees); rest common |
| `README.md` | #2 M | step-4 row + "gh_hugepage_reserve is NOT built by step 4" → #3; step-5 row + turnip paragraph → #4; step-8/9 rows + "Guest mesa" section → #6; route rows in Branches / Guest mesa tables → #12 (drm2kgsl) / #14 (gfxstream); venus row (missing at 9cc6ef4, added by d4a4f3d) → #17 |
| `deploy/SETUP.md` | #7 G | §1 "gfxstream 路線(四種記憶體配置)" → #14; §2 "drm2kgsl native context 路線" + §4 drm2kgsl bullets → #12; §0/§3 common |
| `mesa-variants.sh` | #6 G | `gfxstream` entries in mesa_variant_branch/meson/icd/siblings + `${b%-gfxstream}` → #14; `drm2kgsl`/`mesa-guest-kgsl` entries + `${b%-drm2kgsl}`/`${b%-kgsl}` + the "named for the host translation" comment → #12; `venus` entries + `${b%-venus}` → #17; MESA_COMMON_MESON, mesa_base_branch, mesa_variant_pkg, mesa_worktree, mesa_pkg_version, header → #6. (Gone after d4a4f3d.) |
| `lib_mesa_build.sh` | #6 G | `case $v in gfxstream\|drm2kgsl\|venus)` names → #14/#12/#17 (trivial) |
| `lib_dist.sh` | #6 G | l.51–53 per-variant "still to run" checks → #14/#12/#17 (trivial) |
| `deploy/gfxstream/bench_all.sh` | #8 G | CONFIGS `drm2kgsl:` row (l.25) + `mesa-guest-drm2kgsl mesa-guest-kgsl` in mesa_ensure purge list + the "two variants share 60 paths" comment → #12; the four gfxstream CONFIGS rows → #14 |
| `deploy/gfxstream/mc_bench.sh` | #8 G | launcher selection `launch_mc_kgsl_nctx.sh` for `V = drm2kgsl` (~l.185–204, 231–233) → #12; `/sys/class/kgsl` clock pins are HOST kgsl (common, stay) |
| `deploy/gfxstream/sweep_all_routes.sh` | #8 G | phase 1 (gfx launcher names) → #14; phase 2 (drm2kgsl) → #12 |
| `deploy/gfxstream/bringup.sh` | #7 G | gfx launcher push list (l.68–70) → #14; `run_drm2kgsl_nctx.sh` push + `crosvm_drm2kgsl` dir (l.52, 71) → #12 |
| `deploy/guest/provision.sh` | #7 G | `case $VARIANT` names, `8_build_guest_mesa_$(...)` hint, `Expect driverName=` line, dmesg grep `drm2kgsl_host` → #12/#14 (trivial) |
| `guest-patches/mesa-26.0.3/` | split by file | freedreno files + README → #13 D; `src/gallium/drivers/zink/zink_screen.c`, `src/gfxstream/guest/vulkan_enc/ResourceTracker.cpp` → #15 X |
| `plans/GFX_SHIP_PLAN.md` | #16 X | §1.4 pool-reclaim measurement text duplicates #10 POOL_RECLAIM_PLAN — no split needed, just note the duplication |

### Open questions / decisions for the owner
1. **Which HEAD to flatten from.** d4a4f3d (2026-08-17) moved `mesa-cross/` + `mesa-variants.sh` to Droid-VM/mesa-cross (filter-repo). If pr/3d-accel is built from d4a4f3d: #6 becomes `lib_mesa_build.sh` (now containing `mesa_worktree()` + `clone_at mesa-cross`), `lib_dist.sh`, `9_build_guest_addition.sh` + README/.gitignore bits; commits b61b21b, bc1f0c2 (mesa parts), acbb43c, 8068223, 6b425ef, cb30848, d2cb940, 1abda84, 64d57d1 (build.sh), 82a3c95 (mesa-variants) net to zero here and must be flattened in the mesa-cross repo instead; #12/#14/#17's mesa-variants.sh hunks likewise move there. Recommend d4a4f3d as the flatten head and re-running the coverage against it (180 commits, 101 net files).
2. **Uncommitted refactor in flight**: `plans/POOL_DESIGN.md` deleted in the working tree — moving to gh-hugepage-reserve? If the pool design docs are relocating, drop #10's POOL_* docs and all of #11 from this PR. Untracked `plans/ACCEPTANCE_REPORT_R5_20260817.md`, `CLAUDE.md`, `info.txt` are not in the range.
3. **Branch naming for pr/**: `lib_branch.sh` hard-codes `TRUNK=wip/3d-accel`, `LEGACY=wip/3d-accel-gfxstream`; `mesa_base_branch` strips `-gfxstream/-drm2kgsl/-kgsl/-venus`. On a `pr/3d-accel` meta branch the chain resolves `pr/3d-accel-<variant>` → `pr/3d-accel` → LEGACY, so mesa (and now mesa-cross) need matching `pr/3d-accel-{gfxstream,drm2kgsl,venus}` branches, or TRUNK/LEGACY get parameterised for the PR. Also the org default is `wip-3d-accel` (hyphen) — decide the PR target name.
4. **Base rebase**: `origin/wip-3d-accel` = 4b0a5a3 = commits 1–2 (bf63337 + 4b0a5a3). If the PR targets that branch, their content (start_nctx.sh, root-level *.md, old script names, guest-patches/mesa-26.0.3, GFXSTREAM_ARENA_PLAN v1) is already on the base and the flatten must move/rewrite/delete rather than add. Cleaner: publish pr/3d-accel as a fresh history from the empty tree (or fast-forward the org branch to the flatten).
5. **SPDX headers**: LICENSING.md says DroidVM material is what carries the SPDX tag, but no script/doc in this repo has one. Add `# SPDX-License-Identifier: GPL-3.0-or-later` in #1 (or first touch), and decide the tag for deploy/*.py/.c (vkdisplay_test.c, poolinfo.c) and poolprobe (GPL-2.0, kernel).
6. **guest-patches/ policy**: keep any of it? ICD snapshots (#15, #13) are superseded by mesa branches; linux patches (#5) duplicate the DKMS series; if kept, 0001 must credit lateautumn233.
7. **Stale docs/scripts to refresh before PR** (all flagged above): SETUP.md §1/§4, bringup.sh launcher list, provision.sh usage/venus, run_stream_trace.sh env names, README venus row (fixed by d4a4f3d), CROSVM_3D_LAUNCH_REFERENCE.md.
8. **deploy/ layout**: the harness (#8) and diagnostics (#9) sit under `deploy/gfxstream/` but are route-agnostic; consider `deploy/bench/` + `deploy/diag/` in the flatten (a rename inside #8/#9 is free at flatten time). venus has no `deploy/venus/` launcher — intentional (app/daemon path) or a gap?
9. **Guest-side dependencies not in any repo**: `/home/droidvm/launch_mc_vk.sh`, `launch_mc_kgsl_nctx.sh`, `java_wrap.sh` (used by mc_bench/mc_lib), `scratchpad/devvm.sh` etc. (debugloop.md). Either add them or say where they live.
10. **Hard-coded lab constants** in deploy/ (see "consider dropping" last bullet) — parameterise (`PHONE`/`GUEST`/`QCOW`/`BOOT_UUID`) or accept for a WIP harness.

### Coverage check
- all original commits assigned: **yes** (179/179; verified by script — each hash appears in at least one proposed commit; multi-feature commits are listed under every commit they feed, primary first)
- all net files assigned: **yes** (108/108; every path in `git diff --name-status 4b825dc..9cc6ef4` appears in exactly one primary row above, SPLIT rows listed separately)
- new commit outside the analysed range: d4a4f3d (see Open questions #1)
